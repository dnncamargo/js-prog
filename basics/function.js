/**
 * Blocos de código funcionam com expressões condicionais, 
 * os quais são executados quando a condição é satisfeita.
 * Funções são blocos invocados que podem ou não atribuir a uma variável
 * E comportam-se como um objeto.
 */

function test () {
    // bloco de código
    console.log("Test")
}

// ao nomear uma função, ela pode ser chamada em qualquer lugar
test()